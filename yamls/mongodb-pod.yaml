apiVersion: v1
kind: Pod
metadata:
  name: mongodb
  labels:
    app: db-mongodb
spec:
  volumes:
  - name: db-mongo-vol
    persistentVolumeClaim:
      claimName: db-pvc
  containers:
  - image: mongo
    name: container1
    command:
    - mongod
    - "--bind_ip"
    - "0.0.0.0"
    ports:
    - containerPort: 27017
    volumeMounts:
    - name: db-mongo-vol
      mountPath: /data/db